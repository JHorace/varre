# App target structure mirrors Rust binaries, excluding unfinished `hello_iced`.
add_subdirectory(triangle)
add_subdirectory(mesh_simple)

# Future app-core target (Rust `lib.rs` equivalent) will be defined here.

