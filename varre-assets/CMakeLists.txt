# Workspace subtree matching the Rust crate `varre-assets`.
# This defines CMake structure and stub targets only.

add_subdirectory(codegen)

add_subdirectory(shaders)
add_subdirectory(models)
add_subdirectory(src)
